# Copy this file to `.env` and adjust values to customize how the wrapper/updater run.
# All variables are optional; unset keys fall back to their baked-in defaults.

# Absolute path to a codex-updater checkout you want mirrored (leave unset to clone on demand).
CODEX_WORKSPACE=/absolute/path/to/codex-updater
# Set to 1 to git fetch + re-run scripts/patch-apply.sh inside CODEX_WORKSPACE before each build.
CODEX_WORKSPACE_SYNC=0
# Set to 1 to skip package-manager dependency installs (useful when deps are already present).
CODEX_SKIP_BUILD_DEPS=1

# Wrapper behaviors:
CODEX_WRAPPER_AUTO_UPDATE=1
CODEX_WRAPPER_AUTO_INTERVAL=86400
CODEX_WRAPPER_ALWAYS_UPDATE=0
CODEX_WRAPPER_ALLOW_SUDO=0

# Optional overrides for installation locations:
# CODEX_UPDATER=/custom/path/codex-updater
# CODEX_BIN=/custom/path/codex

# Optional build tunables:
# CODEX_OPT_PRESET=balanced
# CODEX_TARGET_CPU=
# CODEX_USE_SCCACHE=auto
# CODEX_EXTRA_RUSTFLAGS=
# CODEX_CARGO_JOBS=

# If you store the env file somewhere else, point CODEX_ENV_FILE at it.
# CODEX_ENV_FILE=$HOME/.config/codex-updater/.env
