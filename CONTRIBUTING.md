# Contributing

Thanks for hacking on codex-updater! Please:

1. Fork & branch from `main`.
2. Run `make check` (or at least shfmt + shellcheck) before pushing.
3. Add tests or log samples when fixing bugs.

Open a discussion for major changes. PRs welcomed.
